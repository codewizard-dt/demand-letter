---
id: UAT-001
title: 'UAT: AWS SAM TypeScript Monorepo Scaffold'
status: passed
task: TASK-001
created: 2026-06-23
updated: 2026-06-23
---

# UAT-001 — UAT: AWS SAM TypeScript Monorepo Scaffold

implements::[[TASK-001]]

> **Source task**: [[TASK-001]]
> **Generated**: 2026-06-23

---

## Prerequisites

- [ ] Working directory is the project root (`demand-letter/`)
- [ ] `pnpm` is installed and available on `$PATH`
- [ ] Node.js ≥ 20 is installed
- [ ] `pnpm-lock.yaml` exists (generated by `pnpm install` during TASK-001)

---

## Test Cases

---

### UAT-FS-001: Root workspace files exist

- **Scenario**: All mandatory root-level configuration files are present
- **Steps**:
  1. From the project root, verify the following files exist:
     - `package.json`
     - `pnpm-workspace.yaml`
     - `.npmrc`
     - `tsconfig.json`
     - `eslint.config.js`
     - `.prettierrc`
     - `.prettierignore`
     - `template.yaml`
     - `samconfig.toml`
     - `.gitignore`
     - `pnpm-lock.yaml`
- **Expected Result**: All eleven files are present at the project root. No file is missing.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-002: pnpm workspace glob is correct

- **Scenario**: `pnpm-workspace.yaml` declares `packages/*` so all three packages are discovered as workspace members
- **Steps**:
  1. Read `pnpm-workspace.yaml`
  2. Confirm it contains exactly one package glob: `"packages/*"`
- **Expected Result**: File contains `packages:\n  - "packages/*"` (or equivalent YAML). No other globs present.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-003: Root package.json has correct workspace scripts and devDependencies

- **Scenario**: Root `package.json` defines the four recursive workspace scripts and all required devDependencies
- **Steps**:
  1. Read `package.json` at project root
  2. Verify `"private": true`
  3. Verify `"packageManager": "pnpm@9.0.0"`
  4. Verify scripts: `build`, `typecheck`, `lint`, `format` are all present
  5. Verify devDependencies include: `typescript`, `eslint`, `@typescript-eslint/eslint-plugin`, `@typescript-eslint/parser`, `prettier`
- **Expected Result**: All four scripts use `pnpm -r` (`format` uses `prettier --write .`). All five devDependencies are listed.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-004: Root tsconfig.json enables strict mode

- **Scenario**: The root TypeScript config has `"strict": true` so all packages inherit strict mode
- **Steps**:
  1. Read `tsconfig.json` at project root
  2. Confirm `"strict": true` is set in `compilerOptions`
  3. Confirm `"target": "ES2022"` and `"module": "commonjs"`
  4. Confirm `exclude` array contains `"node_modules"`, `"dist"`, and `".aws-sam"`
- **Expected Result**: `strict` is `true`. `target` is `"ES2022"`. `module` is `"commonjs"`. All three exclusions are present.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-005: `packages/api` scaffold is complete

- **Scenario**: The `api` package has all required files with correct package name and dependencies
- **Steps**:
  1. Confirm `packages/api/package.json` exists
  2. Read `packages/api/package.json` and verify:
     - `"name": "@demand-letter/api"`
     - `"main": "dist/index.js"`
     - Scripts: `build`, `typecheck`, `lint`
     - Dependencies: `@aws-sdk/client-bedrock-runtime`, `@aws-sdk/client-s3`, `@prisma/client`
     - DevDependencies: `@types/aws-lambda`, `typescript`
  3. Confirm `packages/api/tsconfig.json` exists and `"extends": "../../tsconfig.json"`
  4. Confirm `packages/api/src/index.ts` exists
- **Expected Result**: All files exist. Package name is `@demand-letter/api`. All three production dependencies present. tsconfig extends root config.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-006: `packages/api/src/index.ts` exports a Lambda handler

- **Scenario**: The api entry point exports a typed Lambda handler so TypeScript can validate handler signature in later tasks
- **Steps**:
  1. Read `packages/api/src/index.ts`
  2. Verify it imports `APIGatewayProxyHandler` from `'aws-lambda'`
  3. Verify it exports a `handler` constant typed as `APIGatewayProxyHandler`
  4. Verify the handler returns `{ statusCode: 200, body: JSON.stringify({ status: 'ok' }) }`
- **Expected Result**: File matches the stub exactly — imports type, exports async handler returning a 200 response object.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-007: `packages/db` scaffold is complete

- **Scenario**: The `db` package has all required files with Prisma tooling and correct scripts
- **Steps**:
  1. Confirm `packages/db/package.json` exists and verify:
     - `"name": "@demand-letter/db"`
     - Scripts: `build`, `typecheck`, `lint`, `db:generate`, `db:migrate`, `db:push`
     - Dependencies: `@prisma/client`
     - DevDependencies: `prisma`, `typescript`
  2. Confirm `packages/db/tsconfig.json` exists and `"extends": "../../tsconfig.json"`
  3. Confirm `packages/db/src/index.ts` exists
  4. Confirm `packages/db/prisma/schema.prisma` exists
- **Expected Result**: All files exist. Package name is `@demand-letter/db`. All three Prisma scripts present. Prisma schema file exists.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-008: `packages/db/prisma/schema.prisma` has correct stub content

- **Scenario**: The Prisma schema defines the generator and datasource so `prisma generate` can be run by later tasks
- **Steps**:
  1. Read `packages/db/prisma/schema.prisma`
  2. Verify it contains a `generator client` block with `provider = "prisma-client-js"`
  3. Verify it contains a `datasource db` block with `provider = "postgresql"` and `url = env("DATABASE_URL")`
- **Expected Result**: Both blocks are present. No model definitions yet (those are added by TASK-002).
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-009: `packages/db/src/index.ts` re-exports Prisma client

- **Scenario**: The db package entry point re-exports `PrismaClient` and the `Prisma` namespace type so `api` can import from it as a workspace dependency
- **Steps**:
  1. Read `packages/db/src/index.ts`
  2. Verify it contains: `export { PrismaClient } from '@prisma/client';`
  3. Verify it contains: `export type { Prisma } from '@prisma/client';`
- **Expected Result**: Both export lines are present. No other content.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-010: `packages/web` scaffold is complete

- **Scenario**: The `web` package has all required Vite/React files so workspace imports resolve in later tasks
- **Steps**:
  1. Confirm `packages/web/package.json` exists and verify:
     - `"name": "@demand-letter/web"`
     - Scripts: `dev`, `build`, `typecheck`, `lint`, `preview`
     - Dependencies: `react`, `react-dom`
     - DevDependencies: `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `typescript`, `vite`
  2. Confirm `packages/web/tsconfig.json` exists with `"extends": "../../tsconfig.json"` and `"jsx": "react-jsx"`
  3. Confirm `packages/web/src/main.tsx` exists
  4. Confirm `packages/web/index.html` exists
  5. Confirm `packages/web/vite.config.ts` exists
- **Expected Result**: All five files exist. Package name is `@demand-letter/web`. JSX mode is `react-jsx`.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-011: `template.yaml` is a valid SAM skeleton

- **Scenario**: The SAM template exists with the required transform, globals, and Stage parameter so later tasks can add resources
- **Steps**:
  1. Read `template.yaml`
  2. Verify `AWSTemplateFormatVersion: '2010-09-09'`
  3. Verify `Transform: AWS::Serverless-2016-10-31`
  4. Verify `Globals.Function.Runtime: nodejs20.x`
  5. Verify `Parameters.Stage` exists with `Default: dev` and `AllowedValues: [dev, staging, prod]`
  6. Verify `Resources:` section exists (may be empty/comment-only — resources added by later tasks)
  7. Verify `Outputs: {}`
- **Expected Result**: All six elements are present. Template is a valid YAML skeleton.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-012: `samconfig.toml` has correct default deployment settings

- **Scenario**: SAM CLI will read `samconfig.toml` for deploy parameters; settings must match the task spec
- **Steps**:
  1. Read `samconfig.toml`
  2. Verify `version = 0.1`
  3. Verify `[default.global.parameters]` section with `stack_name = "demand-letter"`
  4. Verify `[default.build.parameters]` with `cached = true` and `parallel = true`
  5. Verify `[default.deploy.parameters]` with `capabilities = "CAPABILITY_IAM"`, `confirm_changeset = true`, `resolve_s3 = true`, `region = "us-east-1"`
- **Expected Result**: All settings match. Region is `us-east-1`. Stack name is `demand-letter`.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-FS-013: `.gitignore` excludes all required paths

- **Scenario**: `.gitignore` must exclude `node_modules/`, `dist/`, `.aws-sam/`, `.env`, `.env.*` (with exception for `.env.example`), `.aws/`, `samconfig.local.toml`, `.pnpm-store/`, and `.DS_Store`
- **Steps**:
  1. Read `.gitignore`
  2. Confirm each of the following patterns is present:
     - `node_modules/`
     - `.pnpm-store/`
     - `dist/`
     - `.aws-sam/`
     - `.env`
     - `.env.*`
     - `!.env.example`
     - `.aws/`
     - `samconfig.local.toml`
     - `.DS_Store`
- **Expected Result**: All ten patterns are present.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-CLI-001: `pnpm install` resolves all workspace dependencies without errors

- **Scenario**: The lockfile and workspace resolution are valid — `pnpm install` must exit 0 with no resolution errors
- **Steps**:
  1. From the project root, run:
     ```bash
     pnpm install --frozen-lockfile
     ```
  2. Observe exit code and output
- **Expected Result**: Command exits with code 0. No `ERR_PNPM_*` errors or unresolved peer dependency warnings that block installation. `pnpm-lock.yaml` is present and unchanged after the run.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-CLI-002: `pnpm typecheck` passes across all three packages

- **Scenario**: TypeScript strict mode compiles cleanly across `api`, `db`, and `web` packages — zero type errors in the stub files
- **Steps**:
  1. From the project root, run:
     ```bash
     pnpm typecheck
     ```
  2. Observe output for each package
- **Expected Result**: Command exits with code 0. No TypeScript errors reported for any package. All three packages (`@demand-letter/api`, `@demand-letter/db`, `@demand-letter/web`) complete the typecheck step.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-EDGE-001: Each per-package tsconfig extends the root config

- **Scenario**: All three package tsconfigs must use `"extends": "../../tsconfig.json"` so root strict settings are inherited — a missing or wrong extends path breaks strict-mode guarantees
- **Steps**:
  1. Read `packages/api/tsconfig.json` and confirm `"extends": "../../tsconfig.json"`
  2. Read `packages/db/tsconfig.json` and confirm `"extends": "../../tsconfig.json"`
  3. Read `packages/web/tsconfig.json` and confirm `"extends": "../../tsconfig.json"`
- **Expected Result**: All three files have the `extends` key pointing to the project root tsconfig. None override `"strict"` to `false`.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-EDGE-002: `.npmrc` disables shameful hoisting

- **Scenario**: `shamefully-hoist=false` ensures workspace packages do not accidentally depend on hoisted transitive dependencies, enforcing isolation between packages
- **Steps**:
  1. Read `.npmrc`
  2. Verify `shamefully-hoist=false`
  3. Verify `strict-peer-dependencies=false`
- **Expected Result**: Both settings are present.
- [x] Pass <!-- 2026-06-23 -->

---

### UAT-EDGE-003: Three workspace packages are discoverable by pnpm

- **Scenario**: `pnpm list --recursive` must list all three workspace members by their declared package names
- **Steps**:
  1. From the project root, run:
     ```bash
     pnpm list --recursive --depth 0
     ```
  2. Check that the output lists all three workspace packages
- **Expected Result**: Output includes `@demand-letter/api`, `@demand-letter/db`, and `@demand-letter/web`. All three are shown as workspace members.
- [x] Pass <!-- 2026-06-23 -->
